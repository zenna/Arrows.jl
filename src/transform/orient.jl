# Relation direction/functionalization/graphing Given directions to relations

"""Acyclically orient a relatiGeneralizes `invert`"""
function orient(carr)
end

function isdag(carr. linkdirs) end

"Cost associated with this linkdirs in carr"
function cost(carr, linkdirs)::Integer end